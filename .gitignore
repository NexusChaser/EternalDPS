# IDE projects and build output
*.csproj
*.sln
*.user
*.suo
.vs/
.idea/
obj/
bin/

# OS
.DS_Store
Thumbs.db

# NOTE: .meta files are NOT ignored.
# Unity needs them to keep GUIDs stable across machines and versions. Ignoring them
# would make every install of this package regenerate different GUIDs and break the
# references of anyone consuming it.
