# Design documents

These are the working documents behind the package. They are written in Spanish, which is the
working language of the original team.

- `Architecture.md` — what is built and why.
- `ImplementationPlan.md` — in what order, and when each piece is done.

Unity ignores folders ending in `~`, so nothing here is imported into a project.
